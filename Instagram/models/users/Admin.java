public class Admin extends User {

    public Admin(){
        super();
        this.setUserRole(UserRole.ADMIN);
    }

    // Block or Unblock Users
    // CRUD on users
    // CRUD on post / status
}